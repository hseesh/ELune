package cn.daxiang.lyltd.gameserver.module.${table.name};

/**
 * ${table.comment}
 *
 * @author $!{author}
 * @since ${now}
 */
public interface ${table.name.substring(0, 1).toUpperCase()}${table.name.substring(1)}Cmd {

    /**
     * ${table.comment}
     * <pre>
     *     请求：{@code Request}
     *     响应: {@code Response}
     *     推送：{@code Response}
     * </pre>
     */
    byte GET_${table.alias} = 1;

    /**
     * ${table.comment}
     * <pre>
     *     请求：{@code Request}
     *     响应: {@code Response}
     *     推送：{@code Response}
     * </pre>
     */
    byte LEVEL_UP = 2;

    /**
     * ${table.comment}
     * <pre>
     *     请求：{@code Request}
     *     响应: {@code Response}
     *     推送：{@code Response}
     * </pre>
     */
    byte ADVANCE_LEVEL_UP = 2;

    /**
     * 推送${table.comment}信息
     * 推送：{@code ${table.name.substring(0, 1).toUpperCase()}${table.name.substring(1)}Response}
     */
    byte PUSH_${table.alias} = 100;

    /**
     * 推送删除${table.comment}信息
     * 推送：{@code ${table.name.substring(0, 1).toUpperCase()}${table.name.substring(1)}DeleteResponse}
     */
    byte PUSH_DELETE_${table.alias} = 101;
}
