package cn.daxiang.lyltd.gameserver.module.${table.name.substring(0, 1).toLowerCase()}${table.name.substring(1)}.model;


/**
 * ${table.comment}对象
 *
 * @author ${author}
 * @since ${now}
 */
public class ${table.name} {

    #foreach($item in $table.fields)
   /**
    * ${item.description}
    */
    private ${item.alias} ${item.name};
    #end

    public static ${table.name} valueOf(${table.name} ${table.name.substring(0, 1).toLowerCase()}${table.name.substring(1)}) {
        ${table.name} entity = new ${table.name}();
        #foreach($item in $table.fields)
        entity.${item.name} = ${table.name}.${item.getString}();
        #end
        return entity;
    }

}
