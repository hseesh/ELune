package cn.daxiang.lyltd.gameserver.module.${table.name.substring(0, 1).toLowerCase()}${table.name.substring(1)}.model;


/**
 * ${table.description}对象
 *
 * @author ${author}
 * @since ${now}
 */
public class ${table.alias} {

    #foreach($item in $table.pramList)
/**
    * ${item.description}
    */
    private ${item.typeAlias} ${item.name} ${item.defaultValue};
    #end

}
