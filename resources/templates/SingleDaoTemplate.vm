package cn.daxiang.lyltd.gameserver.module.${table.name.substring(0, 1).toLowerCase()}${table.name.substring(1)}.dao;

import cn.daxiang.lyltd.gameserver.core.database.table.${table.name.substring(0, 1).toLowerCase()}${table.name.substring(1)};
import cn.daxiang.lyltd.gameserver.core.oss.type.OperationType;

import java.util.Collection;
import java.util.Map;

/**
 * ${table.comment}
 * @author ${author}
 * @since ${now}
 */
public interface ${table.name.substring(0, 1).toUpperCase()}${table.name.substring(1)}Dao {



    /**
    * 获取${table.comment}
    * @param actorId 玩家ID
    */
    public ${table.name.substring(0, 1).toUpperCase()}${table.name.substring(1)} get${table.name.substring(0, 1).toUpperCase()}${table.name.substring(1)}(long actorId);


}